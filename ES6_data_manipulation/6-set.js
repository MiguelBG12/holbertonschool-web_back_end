// Define a function setFromArray that creates a Set from an array.
function setFromArray(array) {
  // Use the Set constructor to convert the array into a Set.
  return new Set(array);
}

export default setFromArray;
